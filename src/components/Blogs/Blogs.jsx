import React from 'react';
import Navbar from '../Navbar';

export default function Blogs(props){
    return (
        <div>
             <Navbar color={props.color}/>
            This is the start of the blogs
        </div>
    )
}